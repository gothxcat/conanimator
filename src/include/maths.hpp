/*
*   Maths and conversion defs
*/

#ifndef __MATHS
#define __MATHS

#include <stdlib.h>

#define S_TO_USECONDS(s)            ((s)*1000000)
#define MS_TO_USECONDS(ms)          ((ms)*1000)

struct Point {
    int x,y;
};

#endif
